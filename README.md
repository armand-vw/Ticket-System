# Ticket-System
lightweight IT helpdesk web portal built with Flask and SQLite. Create, view, and manage support tickets with automatic keyword-based routing. Features a live dashboard with search, filters, role-based access, and real-time stats. Zero external dependencies beyond Flask — runs on port 8000 with a single command.
